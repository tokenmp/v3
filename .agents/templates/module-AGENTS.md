# <模块名>

> 作用域：`<module/path>`。本文件继承从仓库根目录到本目录之间的所有 `AGENTS.md`。

## 模块职责

- 负责：
- 不负责：
- 所有者：

## 必读文档

- 模块说明：`README.md`
- 架构：`<path>`
- 接口/契约：`<path>`
- 数据/运维：`<path>`

## 对外能力与返回契约

| 能力/导出 | 输入与前置条件 | 返回/错误/副作用 | 稳定性 | 契约来源 |
|---|---|---|---|---|
| `<symbol or endpoint>` |  |  | internal / experimental / stable / deprecated | `<source/schema/test>` |

## 依赖关系与消费者

| 方向 | 模块/资源 | 使用功能 | 依赖方式 | 契约/入口 | 变更后验证 |
|---|---|---|---|---|---|
| 依赖 | `<module>` |  | import / HTTP / event / DB / tool |  |  |
| 被依赖 | `<consumer>` | `<具体功能>` | import / HTTP / event / generated client |  |  |

## 开发与验证

```bash
# install（如适用）

# lint / format

# typecheck

# test

# build
```

- 最小验证：
- 契约测试：
- 集成测试：

## 模块边界

- 允许访问：
- 禁止访问：
- 数据所有权：
- 配置和环境变量：
- Docker 镜像/部署单元：
- 健康检查：

## DO NOT

- **DO NOT** `<禁止事项>` — 原因：`<风险或历史教训>`；正确做法：`<替代方案>`。

## 已知陷阱与历史教训

### <简短标题>

- 症状：
- 根因：
- DO：
- DO NOT：
- 验证：
- 证据：`<source/test/issue/postmortem>`
- 适用范围：

## 文档维护触发器

出现以下变化时同步更新本文件：

- 公开导出、返回结构、错误语义或副作用变化。
- 新增、删除或改变直接依赖及消费者功能。
- 命令、测试、构建、部署或健康检查变化。
- 数据所有权、接口边界或环境变量变化。
- 发现并确认新的非显然陷阱，或旧陷阱已不再适用。
